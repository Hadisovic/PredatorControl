<Project Sdk="Microsoft.NET.Sdk">

  <PropertyGroup>
    <OutputType>WinExe</OutputType>
    <TargetFramework>net10.0-windows</TargetFramework>
    <Nullable>enable</Nullable>
    <UseWindowsForms>true</UseWindowsForms>
    <ImplicitUsings>enable</ImplicitUsings>
    <ApplicationIcon>appicon.ico</ApplicationIcon>
    <ApplicationManifest>app.manifest</ApplicationManifest>
    <ApplicationHighDpiMode>PerMonitorV2</ApplicationHighDpiMode>
    <Version>1.2.6</Version>
    <AssemblyVersion>1.2.6.0</AssemblyVersion>
    <FileVersion>1.2.6.0</FileVersion>
    <IncludeNativeLibrariesForSelfExtract>true</IncludeNativeLibrariesForSelfExtract>
    <EnableCompressionInSingleFile Condition="'$(SelfContained)' == 'true'">true</EnableCompressionInSingleFile>
  </PropertyGroup>

  <ItemGroup>
    <PackageReference Include="Microsoft.Web.WebView2" Version="1.0.3650.58" GeneratePathProperty="true" />
    <PackageReference Include="System.Management" Version="10.0.0-preview.*" />
    <EmbeddedResource Include="$(PkgMicrosoft_Web_WebView2)\runtimes\win-x64\native\WebView2Loader.dll" LogicalName="WebView2Loader.dll" />
    <PackageReference Include="System.ServiceProcess.ServiceController" Version="10.0.12" />
  </ItemGroup>

  <ItemGroup>
    <Content Include="appicon.ico" />
  </ItemGroup>

  <ItemGroup>
    <None Update="appicon.ico">
      <CopyToOutputDirectory>PreserveNewest</CopyToOutputDirectory>
    </None>
  </ItemGroup>

  <Target Name="RemoveUnusedWebViewWpf" BeforeTargets="ResolveAssemblyReferences">
    <ItemGroup><Reference Remove="@(Reference)" Condition="'%(Reference.Filename)' == 'Microsoft.Web.WebView2.Wpf'" /></ItemGroup>
  </Target>
  <Target Name="BuildJelliFrontend" BeforeTargets="BeforeBuild" Condition="'$(SkipJelliBuild)' != 'true'">
    <Exec WorkingDirectory="$(MSBuildProjectDirectory)/../jelli-ui" Command="npm ci --no-audit --no-fund" Condition="!Exists('$(MSBuildProjectDirectory)/../jelli-ui/node_modules/.package-lock.json')" />
    <Exec WorkingDirectory="$(MSBuildProjectDirectory)/../jelli-ui" Command="npm run build" />
  </Target>
  <Target Name="IncludeJelliAssets" BeforeTargets="AssignTargetPaths;PrepareResources" DependsOnTargets="BuildJelliFrontend">
    <ItemGroup>
      <_JelliAsset Include="../jelli-ui/dist/**/*" />
      <EmbeddedResource Include="@(_JelliAsset)" LogicalName="JelliUI/%(_JelliAsset.RecursiveDir)%(_JelliAsset.Filename)%(_JelliAsset.Extension)" />
    </ItemGroup>
  </Target>
</Project>
